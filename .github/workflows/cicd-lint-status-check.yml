name: ✅CICD Lint Status Check
run-name: ✅CICD Lint Status Check


defaults:
  run:
    shell: pwsh


on:
  pull_request_target:
    branches: main


jobs:
  cicd_lint_status_check:
    name: CICD Lint Status Check
    runs-on: ubuntu-latest
    steps:
    - name: bad stuff
      run: echo "I have done some bad stuff";
